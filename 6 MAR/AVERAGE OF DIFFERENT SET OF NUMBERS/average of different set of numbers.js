let add_numbers = [15, 25]; // enter two numbers
let divide_numbers = add_numbers[0] + add_numbers[1];

if (add_numbers[0] + add_numbers[1] === divide_numbers) {
    console.log(divide_numbers / 2); // divide by the number of numbers
} else {
    console.log("error");
}